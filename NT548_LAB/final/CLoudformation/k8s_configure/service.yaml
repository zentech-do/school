apiVersion: v1
kind: Service
metadata:
  name: service-fastapi-placeholder
  namespace: fastapi-placeholder
spec:
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: app-fastapi-placeholder
